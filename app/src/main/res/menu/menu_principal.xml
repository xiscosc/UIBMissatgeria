<menu xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    tools:context="com.fsc.uibmissatgeria.ui.activities.PrincipalActivity">
    <item
        android:title="@string/profile_edit"
        android:id="@+id/menu_edit_profille"
        android:icon="@drawable/user_avatar_menu"
        app:showAsAction="ifRoom"/>
    <item
        android:title="@string/action_settings"
        android:id="@+id/action_settings"
        android:onClick="loadSettings"
        android:icon="@drawable/ic_action_action_settings"
        app:showAsAction="ifRoom"/>
</menu>
